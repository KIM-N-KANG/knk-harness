---
name: create-branch
description: Create a Git branch from a Jira ticket using this repository's KNK branch naming convention. Use when asked to create, checkout, start, or prepare a branch for a Jira issue such as KNK-123. Prefer Atlassian MCP to verify the Jira issue and obtain its summary; fall back to a user-provided title when Jira is unavailable.
---

# Create Branch

Use the shared repository skill at `.agents/skills/create-branch/SKILL.md`.

Follow that workflow exactly. Use its script at `.agents/skills/create-branch/scripts/create_branch.py` to preview or create branch names.

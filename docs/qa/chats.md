# QA — 채팅 (채팅 목록 · 채팅 화면)

| 항목      | 값                                                                                                                                                                                |
| --------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 화면      | 채팅 목록 `/chats`(FE-SCREEN-004), 채팅 화면 `/chats/[id]`(FE-SCREEN-005), 공유 열람 `/share/[shareId]`                                                                          |
| 관련 스펙 | [`3-1-client.md §3-1-3·§3-1-5·§3-1-6`](../product-specs/3-1-client.md), [`2-user-stories.md §2-5·§2-6`](../product-specs/2-user-stories.md)                                             |
| 관련 E2E  | `manyak-web/e2e/chats/chat-list.spec.ts`, `manyak-web/e2e/chats/chat-room.spec.ts`, `manyak-web/e2e/chats/chat-tour.spec.ts`, `manyak-web/e2e/visual/chats-visual.spec.ts`, `manyak-web/e2e/chats/chat-guest-limit.spec.ts`, `manyak-web/e2e/chats/chat-share.spec.ts`, `manyak-web/e2e/share/shared-chat.spec.ts`, `manyak-web/e2e/visual/share-visual.spec.ts` |
| 기준 코드 | `manyak-web` dev HEAD. `미배포` 표기 케이스는 v0.2.2 릴리스에 미포함(응답 재생성)                |

컬럼 정의와 우선순위 기준은 [`README.md`](./README.md)를 따릅니다.

## CHAT-LIST — 채팅 목록 `/chats`

게스트는 `localStorage`(`manyak:created-chat-ids`)의 ID를 `POST /chats/batch`로 배치 조회하고, 회원은 `GET /users/me/chats` 서버 목록을 사용합니다. 두 모드 모두 서버 응답 순서(최근 활동순)를 유지합니다.

| ID           | P   | 사전조건                                                     | 절차                                 | 기대 결과                                                                                                           | 자동화                       | 근거                          |
| ------------ | --- | ------------------------------------------------------------ | ------------------------------------ | ------------------------------------------------------------------------------------------------------------------- | ---------------------------- | ----------------------------- |
| CHAT-LIST-01 | P0  | 게스트, 로컬에 채팅 ID 2개 이상                              | `/chats` 진입                        | 배치 조회로 채팅 카드 목록 표시. 서버 응답 순서(최근 활동순) 유지                                                   | ✅ e2e `chats/chat-list`     | US-5-1, §3-1-3                  |
| CHAT-LIST-02 | P1  | CHAT-LIST-01                                                 | 카드 구성 확인                       | 내부 패딩 가로 16px·세로 8px. 12px 곡률의 썸네일 자리(placeholder) · 스토리 제목 + 제목 줄 오른쪽 끝 옵션 버튼(`icon-xs` 24px, 세로 중심이 제목 행 중심보다 1px 위 — 잉크 보정) · 마지막 장면 미리보기 · 턴 수 · 상대 시간 표시 | ✅ e2e `chats/chat-list`·`visual/chats-visual` | US-5-1, §3-1-3, KNK-994·1186    |
| CHAT-LIST-17 | P1  | CHAT-LIST-01                                                 | 카드 옵션 버튼 탭                    | 화면 가운데 다이얼로그. 상단 회색 상자에 그 채팅 카드의 축소판(다른 카드 내용 없음), 아래 항목은 게스트 "삭제하기"만 / 회원 "신고하기" → "삭제하기" | ✅ e2e `chats/chat-list`     | §3-1-3 FE-SCREEN-004, KNK-1186  |
| CHAT-LIST-18 | P0  | 게스트, 채팅 카드 2개                                        | 옵션 다이얼로그 → "삭제하기" → 같은 창의 "채팅을 삭제할까요?"("삭제하면 나눈 이야기가 모두 사라지며 되돌릴 수 없어요") 확정 | "채팅이 삭제되었어요" 토스트 + 이동 없이 그 카드만 목록에서 제거(로컬 ID 선제거, 나머지 카드 유지) | ✅ e2e `chats/chat-list`     | §3-1-3·§3-1-6, KNK-1186         |
| CHAT-LIST-19 | P1  | 회원 채팅 카드                                               | 옵션 다이얼로그 → "신고하기"        | 옵션 다이얼로그가 닫히고 스토리 신고 시트가 열림. 대상은 카드의 참조 스토리 `storyId`(`POST /stories/{storyId}/reports`). 시트 동작은 STORY-DETAIL-29~31 | ✅ e2e `chats/chat-list`     | §3-1-3 스토리 신고, KNK-1186    |
| CHAT-LIST-03 | P0  | CHAT-LIST-01                                                 | 카드 탭                              | 해당 채팅 화면(`/chats/{id}`)으로 이동                                                                              | ✅ e2e `chats/chat-list`     | US-5-2                        |
| CHAT-LIST-04 | P1  | 게스트, 로컬 채팅 ID·스토리 ID 모두 없음(온보딩은 열람 처리) | `/chats` 진입                        | "아직 진행중인 채팅이 없어요" + "스토리 만들기" CTA(`/studio/story/simple`)                                               | ✅ e2e `chats/chat-list`     | US-5-4, §3-1-3                  |
| CHAT-LIST-05 | P1  | 게스트, 채팅 ID 없음 + 스토리 ID 있음                        | `/chats` 진입                        | 같은 빈 안내 + "스토리 목록으로 가기" CTA(`/studio`)                                                                 | ✅ e2e `chats/chat-list`     | US-5-4, §3-1-3                  |
| CHAT-LIST-06 | P1  | 게스트, 채팅 ID 있음                                         | 배치 조회가 5xx로 실패               | "채팅을 불러오지 못했어요" + "다시 시도하기" 버튼. 재시도 성공 시 목록 표시                                               | 수동                         | US-2-6 유형, §3-1-3             |
| CHAT-LIST-07 | P2  | 게스트, 채팅 ID 있음                                         | 진입 직후(하이드레이션·조회 중) 관찰 | 빈 상태 깜빡임 없이 실제 카드와 같은 가로 16px·세로 8px 패딩, 48px·12px 곡률 썸네일, 제목·미리보기·메타 행의 지연 스켈레톤 → 목록 전환 | ✅ e2e `chats/chat-list`      | §3-1-3·§3-1-6 SSR 3-state, KNK-1043 |
| CHAT-LIST-08 | P2  | 게스트, 로컬 ID 중 일부가 서버에서 삭제됨                    | `/chats` 진입                        | 서버가 반환하지 않은 ID는 조용히 카드에서 제외                                                                      | 수동                         | §3-1-6 배치 재조회              |
| CHAT-LIST-09 | P2  | `lastStoryPreview`가 빈 문자열인 채팅                        | `/chats` 진입                        | 미리보기 자리에 "채팅을 시작하고 이야기를 이어가 보세요" fallback 표시                                              | ✅ e2e `visual/chats-visual` | §3-1-3                          |
| CHAT-LIST-10 | P2  | `lastStoryPreview`가 `null`인 채팅                           | `/chats` 진입                        | 해당 채팅은 목록 변환 단계에서 제외되어 카드가 나타나지 않음                                                        | 수동                         | §3-1-3                          |
| CHAT-LIST-16 | P1 | 참조 스토리가 삭제된 채팅(`storyTitle`이 빈 문자열) | `/chats` 진입 → 카드 옵션 탭 | 카드가 목록에 남고 제목 자리에 보조색으로 "삭제된 스토리" 표시. 카드 링크의 접근 가능한 이름도 같은 문구를 씀. 옵션 다이얼로그에는 회원이라도 "신고하기" 없음(대상 스토리가 없음) | ◐ e2e `chats/chat-list`(문구·링크 이름) | §3-1-3 화면 구성(채팅 카드), KNK-1092·1186 |
| CHAT-LIST-11 | P0  | 회원 로그인 상태                                             | `/chats` 진입                        | 로컬 ID와 무관하게 `GET /users/me/chats` 서버 목록으로 카드 표시(이관 미발동)                                       | ✅ e2e `chats/chat-list`     | §3-1-6 쓰기·삭제, FE-SCREEN-008 |
| CHAT-LIST-12 | P1  | 회원, 서버 채팅 없음 + 서버 스토리 있음                      | `/chats` 진입                        | 빈 안내 + "스토리 목록으로 가기" CTA(`/studio`)                                                                     | ✅ e2e `chats/chat-list`     | US-5-4                        |
| CHAT-LIST-13 | P2  | 세션 판별 중(`loading`)                                      | `/chats` 진입 직후 관찰              | 게스트 빈 상태가 아닌 로딩으로 표시(회원에게 빈 서재 깜빡임 없음)                                                   | 수동                         | 구현(`use-created-chats`)     |
| CHAT-LIST-14 | P1  | 게스트                                                       | `/chats` 헤더 오른쪽 확인             | 제목 오른쪽 끝에 secondary "로그인" 버튼 표시. 탭하면 `/login`으로 이동                                              | ◐ e2e `smoke/navigation`(노출) | §3-2-3 상단 헤더              |
| CHAT-LIST-15 | P1  | 회원 로그인 상태 또는 세션 판별 중                           | `/chats` 헤더 오른쪽 확인             | 로그인 버튼 없음. 세션 판별 중에도 표시하지 않아 회원에게 버튼이 깜빡이지 않음                                      | ◐ e2e `stories/story-list`(회원 상태만) | §3-2-3 상단 헤더              |

## CHAT-ENTRY — 채팅 화면 진입·렌더

채팅 화면은 `GET /chats/{id}`로 스토리 제목·프롤로그·턴 목록·추천 입력을 조회합니다. 온보딩 게이팅이 없는 `(chat)` 레이아웃입니다.

| ID            | P   | 사전조건                           | 절차                        | 기대 결과                                                                                                       | 자동화                                        | 근거                   |
| ------------- | --- | ---------------------------------- | --------------------------- | --------------------------------------------------------------------------------------------------------------- | --------------------------------------------- | ---------------------- |
| CHAT-ENTRY-01 | P0  | 턴 0개 채팅                        | 진입                        | 프롤로그 버블 + 추천 입력(`suggestedInputs`) 버튼 표시                                                          | ✅ e2e `chats/chat-room`                      | US-6-1, §3-1-5           |
| CHAT-ENTRY-02 | P0  | 턴 2개 이상 채팅                   | 진입                        | 프롤로그 → 각 턴(사용자 버블 → AI 버블) 순서 렌더. 선택지는 마지막 턴에만 표시                                  | ✅ e2e `visual/chats-visual`                  | US-6-1, §3-1-5 턴 모델   |
| CHAT-ENTRY-03 | P1  | 턴 있는 채팅                       | 진입 직후 스크롤 위치 확인  | 마지막 메시지 위치에서 시작. 턴 0개면 프롤로그가 보이는 상단에서 시작                                           | 수동                                          | §3-1-5 렌더와 스크롤     |
| CHAT-ENTRY-04 | P1  | 상세 조회가 5xx로 실패             | 진입                        | "채팅을 불러오지 못했어요" + "다시 시도하기". 재시도 성공 시 화면 표시                                                | ◐ e2e `visual/chats-visual`(실패 화면 렌더만) | US-6-8, §3-1-5 예외 처리 |
| CHAT-ENTRY-05 | P2  | 긴 대화 이력                       | 위로 스크롤 → 아래로 스크롤 | 헤더가 위로 스크롤 시 표시, 아래로 스크롤 시 숨김(`aria-hidden`·`inert`). 스크롤된 위치로 진입하면 숨긴 채 시작 | ◐ e2e `chats/chat-room`(고정만)               | §3-1-5 렌더와 스크롤     |
| CHAT-ENTRY-06 | P1  | 임의 채팅                          | 헤더 뒤로가기 탭            | `/chats`(채팅 목록)로 이동                                                                                      | 수동                                          | §3-1-5                   |
| CHAT-ENTRY-07 | P2  | AI 출력에 `**볼드**`·`*강조*` 포함 | 본문 확인                   | `**볼드**`는 굵게, `*...*`는 서술·독백 강조로 렌더                                                              | ✅ e2e `visual/chats-visual`                  | US-6-5, §3-1-5           |
| CHAT-ENTRY-08 | P1  | 회원이 이관되지 않은 게스트 채팅에 진입(상세 조회 403 — 이관 1회 제한 도달 계정 등) | 진입                        | "지금 계정에서는 볼 수 없어요" + "스토리와 채팅을 이미 한 번 옮긴 계정이라 옮기지 못했어요" + "채팅 목록으로 가기" 버튼. 성공할 수 없는 "다시 시도하기"는 제공하지 않음 | ✅ e2e `auth/migration`                       | 백엔드 §4-5 교차 접근 차단, AUTH-MIGRATE-03 |
| CHAT-ENTRY-09 | P1  | 임의 채팅                          | 헤더 우측 구성 확인         | 공유 버튼(share 아이콘)과 옵션 메뉴(⋮) 두 개만 있음. 홈 버튼은 없음(KNK-715에서 공유 버튼으로 대체 — 하단 네비의 홈 탭과 중복이었음). 공유 버튼 동작은 CHAT-SHARE-01 | ✅ e2e `chats/chat-share`                     | §3-1-5, KNK-715          |
| CHAT-ENTRY-11 | P1  | 참조 스토리가 삭제된 채팅(`storyTitle`이 빈 문자열) | 진입 → 옵션 메뉴 탭       | 헤더 제목(h1) 자리에 보조색 "삭제된 스토리". 옵션 메뉴에는 회원이라도 "삭제하기"만(신고 대상 스토리가 없음) | ✅ e2e `chats/chat-room`                      | §3-1-3 FE-SCREEN-005, KNK-1186 |
| CHAT-ENTRY-10 | P2  | 임의 채팅                          | 브라우저 탭 제목 확인       | 데이터 도착 후 `스토리 제목 - 마냑`. 화면을 벗어나면 기본 문서 제목([§3-2-3](../product-specs/3-2-web-app.md))으로 복귀 | ✅ e2e `chats/chat-room`                      | 구현(`chat-room`)      |

## CHAT-TOUR — 채팅 화면 안내 투어

생애 첫 채팅방 진입 시 입력 영역 사용법을 3스텝 스포트라이트로 안내합니다. 열람 여부는 `localStorage`(`manyak:chat-tour-seen`)로 관리하며 기기별 1회만 노출합니다(재열람 진입점 없음). 추천 입력은 스크롤에 따라 화면 밖으로 밀려날 수 있어 투어 대상에서 제외하고, 대신 첫 추천 목록 위 1회성 인라인 힌트로 소개합니다(별도 플래그 `manyak:chat-choices-hint-seen`).

| ID           | P   | 사전조건                                   | 절차                                   | 기대 결과                                                                                                        | 자동화                   | 근거    |
| ------------ | --- | ------------------------------------------ | -------------------------------------- | ------------------------------------------------------------------------------------------------------------------ | ------------------------ | ------- |
| CHAT-TOUR-01 | P1  | 투어 미열람(로컬 플래그 없음), 턴 0개 채팅 | 진입                                   | 잠시 후 "채팅 화면 안내" 투어 1스텝(상황 · 대사 추가) 자동 노출 + 딤·하이라이트. 하이라이트는 상황 추가·대사 추가 버튼만 묶어서 잡고, 입력창은 포함하지 않음. 노출 시점에 재노출 방지 플래그 저장 | ✅ e2e `chats/chat-tour` | KNK-694 |
| CHAT-TOUR-02 | P1  | CHAT-TOUR-01 노출 상태                     | "다음" 2회 → "완료"                    | 상황 · 대사 추가 → 입력 설정 → 랜덤 전송 순서로 하이라이트 이동 후 닫힘                                             | ✅ e2e `chats/chat-tour` | KNK-694 |
| CHAT-TOUR-03 | P1  | CHAT-TOUR-01 노출 상태                     | "건너뛰기"                             | 즉시 닫힘. 새로고침·재진입 시 다시 뜨지 않음                                                                 | ✅ e2e `chats/chat-tour` | KNK-694 |
| CHAT-TOUR-04 | P2  | 투어 미열람, 턴 1개 이상 채팅              | 진입                                   | 자동 노출 없음(플래그도 저장하지 않음 — 이후 턴 0개 방 진입 시 노출)                                          | 수동                     | KNK-694 |
| CHAT-TOUR-05 | P1  | 일반 입력 모드                             | 투어 1스텝 확인                        | 제목이 "상황 추가"로 바뀌고 상황 추가 버튼만 하이라이트. 설명도 강조 마커 삽입 동작으로 안내(입력창 추가 아님) | ✅ e2e `chats/chat-tour` | KNK-694 |
| CHAT-TOUR-06 | P2  | 투어 노출 상태                             | 딤 영역(뒤 화면) 탭                    | 뒤 화면과 상호작용 불가. 투어 카드의 버튼만 동작                                                             | 수동                     | KNK-694 |
| CHAT-TOUR-07 | P2  | 프롤로그가 길어 스크롤이 필요한 채팅       | 진입                                   | 안내 대상(입력 영역)이 항상 화면에 있어, 설명만 뜨고 대상이 안 보이는 상태가 없음                            | 수동                     | KNK-694 |
| CHAT-TOUR-08 | P1  | 힌트 미열람(로컬 플래그 없음), 턴 0개 채팅 | 진입                                   | 추천 입력 목록 위에 인라인 힌트 두 줄 표시("AI가 추천하는 입력이에요" / "탭하면 바로 전송되고, ✏️ 버튼으로 고쳐서 보낼 수 있어요"). 추천과 함께 스크롤되며, 재진입 시 미표시 | ✅ e2e `chats/chat-tour` | KNK-694 |
| CHAT-TOUR-09 | P1  | 태블릿·데스크톱 등 앱 프레임보다 넓은 화면 | 투어 노출 상태 확인                    | 안내 카드가 앱 프레임(`max-w-md`) 좌우 여백 안에 머물고, 위아래로도 화면을 벗어나지 않음                     | ✅ e2e `chats/chat-tour` | KNK-694 |
| CHAT-TOUR-10 | P2  | 임의 채팅                                  | 각 스텝의 카드·하이라이트 정렬 확인    | 카드는 하이라이트 중앙에 오고, 대상이 좌우 끝이면 그쪽 하이라이트 변과 카드 변이 맞음(어긋나 보이지 않음)     | 수동                     | KNK-694 |

## CHAT-SEND — 전송·SSE 스트리밍

`POST /chats/{id}/turns/stream`(SSE: `started → (token | character_image)×N → completed | error`). `character_image`는 `Phase 2 · 구현`(KNK-1013)이며 도착 즉시 현재 스트리밍 위치에 표시합니다. `completed` 수신 시 상세 refetch로 서버 확정 턴을 표시하고 채팅 목록 캐시를 무효화합니다.

| ID           | P   | 사전조건                                   | 절차                                          | 기대 결과                                                                                                               | 자동화                   | 근거                         |
| ------------ | --- | ------------------------------------------ | --------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------- | ------------------------ | ---------------------------- |
| CHAT-SEND-01 | P0  | 일반 입력 모드                             | 텍스트 입력 → 전송                            | 사용자 버블 즉시 표시 → 토큰이 실시간 누적 → 완료 시 서버 확정 턴으로 교체                                              | ✅ e2e `chats/chat-room` | US-6-2·6-3, §3-1-5             |
| CHAT-SEND-02 | P2  | 전송 직후(첫 토큰 전)                      | 관찰                                          | "작성 중" shimmer 로딩(스크린 리더 라벨 "답변을 작성하고 있어요") 표시                                                  | 수동                     | US-6-7, §3-1-5                 |
| CHAT-SEND-03 | P0  | 스트리밍 진행 중                           | 입력창·전송·선택지·재생성 버튼 확인           | 모두 비활성화(전송 잠금). 전송 버튼은 스피너로 바뀜(CHAT-SEND-15). 완료 후 해제                                          | 수동                     | US-6-7, §3-1-5 입력창과 선택지 |
| CHAT-SEND-04 | P1  | 일반 모드, 텍스트 입력됨                   | `Enter` / `Shift+Enter` / IME 조합 중 `Enter` | `Enter`만 전송. `Shift+Enter`는 줄바꿈, IME 조합 중에는 전송 안 됨                                                      | 수동                     | §3-1-5                         |
| CHAT-SEND-05 | P1  | 전송 직후                                  | 토큰 누적 중 화면 관찰                        | 새 턴이 뷰포트 상단에 앵커되고 토큰 누적에도 화면이 끌려 내려가지 않음. 스트리밍 중 옮긴 스크롤 위치는 완료 후에도 유지 | ◐ e2e `chats/chat-scroll`(완료 시 유지만) | US-6-6, §3-1-5 렌더와 스크롤   |
| CHAT-SEND-06 | P2  | 스크롤이 하단이 아님                       | 관찰                                          | "맨 아래로 이동" 버튼 표시. 스트리밍 중에도 동일(스피너 대체 없음)                                                      | 수동                     | §3-1-5                         |
| CHAT-SEND-07 | P0  | 서버가 `error` 이벤트 응답                 | 전송                                          | "응답 생성에 실패했어요" 토스트 + 낙관적 사용자 버블 제거. 재전송 가능                                                  | ✅ e2e `chats/chat-room` | US-6-8, §3-1-5 예외 처리       |
| CHAT-SEND-08 | P1  | 스트림 중 네트워크 절단                    | 전송 후 오프라인 전환                         | 실패 토스트 + 낙관적 버블 제거. 자동 재시도 없음                                                                        | 수동                     | §3-1-5 예외 처리               |
| CHAT-SEND-09 | P2  | 스트리밍 진행 중                           | 뒤로가기 등으로 방 이탈                       | 진행 중 스트림 abort. 토스트 없이 조용히 종료                                                                           | 수동                     | §3-1-5 예외 처리               |
| CHAT-SEND-10 | P2  | 전송 완료 후                               | `/chats` 목록 복귀                            | 카드의 미리보기·상대 시간이 방금 턴으로 갱신됨(게스트·회원 목록 캐시 모두 무효화)                                       | 수동                     | §3-1-5 SSE 계약                |
| CHAT-SEND-11 | P1  | 스트림이 `completed`·`error` 없이 EOF 종료 | 전송                                          | 실패 토스트 + 스트리밍 상태 해제(전송 버튼 스피너도 원복). 상세 refetch로 서버가 저장한 턴이 있으면 화면에 나타남      | ✅ e2e `chats/chat-room` | §3-1-5 예외 처리               |
| CHAT-SEND-12 | P1  | 스트리밍 중 아래(여백 영역)로 스크롤함     | 스트림 완료 시점 화면 관찰                    | 스트리밍 블록이 확정 턴으로 교체될 때 스크롤이 위로 튀지 않음(한 프레임 수축 없음)                                      | ✅ e2e `chats/chat-scroll` | §3-1-5 렌더와 스크롤           |
| CHAT-SEND-13 | P1  | 스트림 완료 후 하단 여백(앵커 스페이서) 잔존 | 위로 스크롤 → 다시 맨 아래로 스크롤           | 위로 스크롤한 만큼 여백이 회수되어, 다시 내리면 콘텐츠 끝에서 스크롤이 멈추고 빈 공간이 없음                            | ✅ e2e `chats/chat-scroll` | §3-1-5 렌더와 스크롤           |
| CHAT-SEND-14 | P1  | 토큰 수신 완료, 확정 턴(선택지) 대기 중      | 맨 아래(여백 영역)로 스크롤 후 도착 대기      | 선택지 포함 확정 턴이 도착해도 보고 있던 본문이 위로 밀리지 않고, 늘어난 만큼 여백이 즉시 회수돼 스크롤 길이도 늘지 않음 | ✅ e2e `chats/chat-scroll` | §3-1-5 렌더와 스크롤           |
| CHAT-SEND-15 | P1  | 전송 또는 응답 재생성으로 스트리밍 중        | 전송 버튼 확인                                | 전송·랜덤 전송 아이콘 대신 스피너 표시(스크린 리더 라벨 "응답을 받는 중"). 블럭·일반 모드 동일하며 완료 시 원래 아이콘 복귀 | ✅ e2e `chats/chat-room` | KNK-694                      |
| CHAT-SEND-16 | P1  | 엔딩에 도달한 턴이 마지막인 채팅(`reachedEnding` 존재)       | AI 응답·입력창·선택지 확인 → 다음 턴 전송      | 확정 AI 응답의 가장 위 왼쪽에 primary 옅은 배경의 `엔딩 · {이름}` 배지가 표시된다. 입력창·선택지는 일반 턴과 동일하게 동작하고 이후 턴은 배지 없는 일반 턴으로 이어진다. 단 도달 턴에는 "다시 생성" 버튼이 없다(CHAT-REGEN-07) | ✅ e2e `chats/chat-room`·`visual/chats-visual` | US-6-13·14, §3-1-5, KNK-1012 |
| CHAT-SEND-17 | P1  | 프롤로그·사용자 입력·AI 출력 존재            | 글자와 메시지 영역 간격 확인                   | 본문은 16px MaruBuri·자간 -2%·행간 175%(28px), 사용자·AI 메시지는 위·아래 20px·좌우 16px 패딩. 스트리밍 "작성 중"도 같은 MaruBuri 행간 비율 | ✅ e2e `chats/chat-room` | §3-1-5 렌더와 스크롤, KNK-1029 |

## CHAT-IMAGE — 스트리밍 인물 이미지 `Phase 2 · 구현`

AI·백엔드는 이미지가 있는 인물의 대사 앞에서 `character_image {name, imageUrl}`를 먼저 보내고, 완료·상세·공유 `aiOutput`의 같은 위치에는 `[[URL]]` 저장 마커를 둡니다. 웹은 두 입력을 같은 `text | character_image` 조각 렌더 모델로 표시하고, 허용 CDN·전용 줄·빈 줄 하나·뒤따르는 인물명 대사 라벨을 만족하는 값만 이미지로 해석합니다(KNK-1013·1015).

| ID            | P   | 사전조건                                      | 절차                                              | 기대 결과                                                                                                                                               | 자동화 | 근거                         |
| ------------- | --- | --------------------------------------------- | ------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ | ---------------------------- |
| CHAT-IMAGE-01 | P0  | `token("앞 문장") → character_image → token("인물명: 대사")` 스트림 | `completed` 전 진행 화면 관찰                     | 앞 문장 → 이미지 → 인물명·대사 순서. 이미지 이벤트가 도착하면 완료를 기다리지 않고 가로 4:3 영역이 즉시 생기며, 뒤의 글자는 이미지 다음에서 계속 토큰 단위로 나타남 | ✅ e2e `chats/chat-room` | US-6-3·6-11, §3-1-5          |
| CHAT-IMAGE-02 | P1  | 같은 `name`·`imageUrl`의 이벤트가 한 턴에 2회 | 두 번째 이벤트까지 수신                           | 이름 기준으로 중복 제거하지 않고 두 위치에 같은 이미지를 각각 표시                                                                                      | ✅ unit `chat-message-segments` | §3-1-5 인물 이미지 렌더      |
| CHAT-IMAGE-03 | P0  | 이미지 이벤트가 포함된 스트림                 | `completed` 수신·상세 refetch 완료 프레임 관찰    | 진행 중 이미지가 확정 마커 기반 이미지로 한 번에 교체되어 사라졌다 다시 나타나거나 중복되지 않음. 마커 글자와 URL 원문은 어느 프레임에도 노출되지 않음    | ✅ e2e `chats/chat-room` | §3-1-5 SSE 계약              |
| CHAT-IMAGE-04 | P0  | 상세 턴 `aiOutput`에 `[[URL]]` 존재           | 채팅방 직접 진입 또는 새로고침                    | 마커 위치에 이미지, 그 뒤에 인물명·대사를 표시. 같은 마커가 여러 번이면 횟수대로 표시하고 마커 글자·URL은 숨김                                            | ✅ e2e `chats/chat-room` + unit `chat-message-segments` | §3-1-5 인물 이미지 렌더      |
| CHAT-IMAGE-05 | P1  | 이미지 URL 로드 실패                          | 스트리밍·상세 화면 각각 관찰                      | 실패한 이미지 블록만 제거. 깨진 이미지·대체 텍스트·URL 원문 없이 앞뒤 본문과 이후 스트리밍은 계속 표시                                                  | 수동   | §3-1-5 인물 이미지 렌더      |
| CHAT-IMAGE-06 | P1  | 이미지 표시 뒤 `error` 또는 저장되지 않은 EOF | 오류 종료                                        | 실패 토스트 후 낙관적 사용자 버블과 미확정 텍스트·이미지를 함께 제거. EOF는 상세 refetch에 저장 턴이 있을 때만 저장 마커 이미지로 복원                   | 수동   | §3-1-5 예외 처리             |
| CHAT-IMAGE-07 | P1  | 유효한 인물 저장 마커가 포함된 공유 링크       | `/share/[shareId]` 열람                           | 저장 마커 글자와 URL은 숨기고, 원본 채팅과 같은 위치에 인물 이미지와 뒤따르는 본문을 함께 렌더                                                            | ✅ e2e `share/shared-chat` | §3-1-5 인물 이미지 렌더      |
| CHAT-IMAGE-08 | P1  | 마지막 확정 턴에 인물 이미지 존재             | "다시 생성" → 이미지 이벤트 수신 → 완료 또는 `error` | 재생성 중 기존 본문·이미지를 스트리밍 조각으로 대체하고 이미지를 즉시 표시. 완료면 새 마커 결과로 교체하고, `error`면 기존 본문·이미지를 함께 복원        | 수동   | §3-1-5 응답 재생성           |
| CHAT-IMAGE-09 | P1  | 이미지 앞뒤에 텍스트가 있는 확정·스트리밍 턴  | 이미지와 텍스트 경계·간격 비교                   | 이미지는 AI 메시지 양쪽에서 16px씩 띄우고 20px 곡률·전체 1px 보더를 적용. 앞 텍스트와 40px, 뒤 텍스트와 20px 간격이며 텍스트·엔딩 배지와 좌우 기준선이 같음 | ✅ e2e `chats/chat-room` | §3-1-5 레이아웃·접근성, KNK-1029 |
| CHAT-IMAGE-10 | P0  | 외부 호스트·잘못된 경로의 URL이 이벤트 또는 저장 마커에 포함됨 | 스트리밍·상세·공유 화면 관찰 | 이미지 최적화 요청을 만들지 않음. 스트림 이벤트는 무시하고, 저장 마커 모양 문자열은 제거하지 않고 일반 본문으로 유지                                           | ✅ unit `chat-message-segments` + e2e `share/shared-chat` | §3-1-5 저장 마커·§3-2-5 원격 이미지 |
| CHAT-IMAGE-11 | P1  | 허용 CDN URL이지만 마커가 독립 줄이 아니거나 뒤에 인물명 대사 라벨이 없음 | 상세·공유 화면 관찰 | 이미지로 해석하지 않고 마커 모양 문자열과 나머지 내용을 일반 본문으로 유지                                                                        | ✅ unit `chat-message-segments` | §3-1-5 저장 마커             |
| CHAT-IMAGE-12 | P0  | 오리지널 스토리 인물 이미지가 `/characters/originals/**` URL로 전달됨 | 스트리밍 수신 → 완료·상세 refetch | 스트리밍 중 이미지가 즉시 표시되고 확정 뒤에도 같은 위치에 유지되며, 저장 마커 글자와 URL은 노출되지 않음                                         | ✅ e2e `chats/chat-room` + unit `chat-message-segments` | §3-1-5 인물 이미지 렌더, KNK-1055 |

## CHAT-INPUT — 입력창·추천 입력·선택지

추천 입력 소스는 턴 0개면 상세의 `suggestedInputs`, 턴이 있으면 마지막 턴의 `choices`입니다.

| ID            | P   | 사전조건                   | 절차                                 | 기대 결과                                                                               | 자동화                   | 근거                 |
| ------------- | --- | -------------------------- | ------------------------------------ | --------------------------------------------------------------------------------------- | ------------------------ | -------------------- |
| CHAT-INPUT-01 | P0  | 추천 입력 표시됨           | 추천 본문 탭                         | 즉시 전송. 상황(`*...*`)·대사를 빈 줄로 구분한 정규화 포맷으로 전송                     | ✅ e2e `chats/chat-room` | US-6-4, §3-1-5         |
| CHAT-INPUT-02 | P1  | 추천 입력 표시됨           | "채우기"(입력창에 넣어 수정) 버튼 탭 | 전송하지 않고 입력창(블럭 모드는 파싱해 블럭별)에 채움                                  | ✅ e2e `chats/chat-room` | US-6-4, §3-1-5         |
| CHAT-INPUT-03 | P2  | 입력창에 작성 중 내용 있음 | 추천 "채우기" 탭                     | "작성 중인 내용을 바꿀까요?" 확인 다이얼로그. "바꾸기"만 교체, "그대로 두기"는 유지     | 수동                     | 구현(`chat-room`)    |
| CHAT-INPUT-04 | P1  | 토글 on + 입력 없음 + 추천 있음 | 전송 버튼 확인·탭                    | filled 재생 아이콘으로 활성화. 탭하면 추천 중 하나를 무작위 즉시 전송(별도 스피너 없음) | ✅ e2e `chats/chat-room` | §3-1-5 입력창과 선택지 |
| CHAT-INPUT-05 | P2  | 토글 on + 입력 없음 + 추천 없음 | 전송 버튼 확인                       | 같은 재생 아이콘 유지한 채 비활성화. 텍스트를 입력하면 위쪽 화살표 전송 버튼으로 활성화 | ✅ e2e `chats/chat-room` | §3-1-5                 |
| CHAT-INPUT-06 | P1  | 턴 1개 이상 진행           | 추천 입력 확인                       | 마지막 턴의 `choices`가 선택지로 표시(이전 턴에는 없음). 공백뿐인 입력은 전송되지 않음  | 수동                     | §3-1-5 턴 모델         |
| CHAT-INPUT-07 | P1  | 일반 모드, 텍스트 선택     | "상황 추가" 버튼 탭                  | 선택 영역이 `*...*`로 감싸짐                                                            | 수동                     | US-6-5, §3-1-5         |
| CHAT-INPUT-08 | P2  | 일반 모드                  | 여러 줄 장문 입력                    | 텍스트에어리어가 최대 `20dvh`까지 자동 확장                                             | 수동                     | §3-1-5                 |
| CHAT-INPUT-09 | P0  | 추천 입력 토글 on(기본)    | 턴 전송 → 완료 대기                  | 본문 확정 후 선택지 자리에 스켈레톤 칩 3개 → 선택지 3개로 교체(`POST …/turns/{turnId}/choices` 발생, 렌더는 상세 재조회의 `turns[].choices`) | ✅ e2e `chats/chat-room` | §3-1-5 추천 입력 토글  |
| CHAT-INPUT-10 | P0  | 추천 입력 토글 off         | 턴 전송 → 완료 대기                  | 선택지 생성 호출 없음. 선택지·스켈레톤 미표시, 직접 입력으로 계속 진행 가능             | ✅ e2e `chats/chat-room` | §3-1-5 추천 입력 토글  |
| CHAT-INPUT-11 | P1  | 토글 off로 턴 종료         | 추천 입력 메뉴(AI 말풍선 아이콘, 모드 메뉴 왼쪽) 탭 → "추천 입력 켬" 선택 | 드롭다운에 켬/끔 라디오 항목(라벨+설명) 표시, 현재 값 체크. 켜는 즉시 마지막 턴 선택지 생성 호출 → 선택지 표시. on이면 트리거 아이콘이 primary 색 | ✅ e2e `chats/chat-room` | §3-1-5 추천 입력 토글  |
| CHAT-INPUT-12 | P1  | 토글 on, 생성 API 실패     | 턴 전송 → 완료 대기                  | 선택지 자리에 "선택지를 만들지 못했어요" + "다시 시도" 버튼. 재시도 성공 시 선택지 표시. 토스트 없음 | ✅ e2e `chats/chat-room` | §3-1-5 추천 입력 토글  |
| CHAT-INPUT-13 | P2  | 임의 상태                  | 토글 값 변경 후 새로고침·재진입      | 변경값이 기기 단위로 유지(localStorage `manyak:chat-choices-enabled`, 기본 on). 저장 값이 손상돼도 기본(on)으로 동작 | 수동                     | §3-1-5 추천 입력 토글  |
| CHAT-INPUT-14 | P1  | 토글 on, 마지막 턴 존재    | 응답 재생성 → 완료 대기              | 본문 교체 후 선택지 재생성 호출 → 새 선택지 표시(기존 choices는 서버가 비움)            | 수동                     | §3-1-5·§4-3-3          |
| CHAT-INPUT-15 | P1  | 선택지 표시 중             | 메뉴에서 "추천 입력 끔" 선택         | 표시된 선택지 즉시 숨김(무작위 추천 전송도 비활성). 다시 켜면 재호출 없이 재표시        | ✅ e2e `chats/chat-room` | §3-1-5 추천 입력 토글  |
| CHAT-INPUT-16 | P1  | 토글 off + 입력 없음       | 전송 버튼 확인                       | 추천이 있어도 재생 아이콘 없이 위쪽 화살표 아이콘으로 비활성화(무작위 전송 불가). 텍스트를 입력하면 활성화 | ✅ e2e `chats/chat-room` | §3-1-5 추천 입력 토글  |
| CHAT-INPUT-17 | P2  | 추천 입력 표시됨           | 네 경로로 각각 전송 후 요청 본문 확인(개발자 도구 네트워크 탭) | 턴 요청 본문의 `userSource`가 입력 방식과 일치 — 추천 본문 탭·무작위 전송은 `choice`, "채우기" 후 그대로 전송도 `choice`, 채운 뒤 고쳐서 전송은 `edited_choice`, 채우기 없이 직접 입력은 `typed`. 화면 동작에는 영향이 없다 | ✅ e2e `chats/chat-room` | §3-1-7, KNK-757 |
| CHAT-INPUT-18 | P1  | 직전 턴 선택지 표시됨     | 두 번째 선택지를 바로 전송·무작위 전송·"채우기" 후 그대로 전송·"채우기" 후 수정 전송하고, 선택지 없이 직접 입력도 수행해 요청 본문 확인 | 선택지를 사용한 경로는 그대로·수정 여부와 무관하게 `sourceTurnId=직전 턴 ID`, `choiceOrder=화면 인덱스+1`을 함께 보낸다. 직접 입력은 두 필드를 보내지 않는다 | ✅ e2e `chats/chat-room` | §3-1-7, KNK-822 |
| CHAT-INPUT-19 | P2  | 추천 입력 표시됨           | 목록 상단과 추천 입력 본문 확인            | 목록은 위 콘텐츠에서 12px 띄우고, 추천 입력 본문은 14px MaruBuri·자간 -2%·행간 175%(24.5px)를 사용 | ✅ e2e `chats/chat-room` | §3-1-5 입력창과 선택지, KNK-1029 |
| CHAT-INPUT-20 | P1  | 회원 채팅방                | 블럭·일반 모드에서 전송 버튼 확인          | 전송 버튼 바로 왼쪽 8px에 `N 이프`(N은 `GET /credits/policies`의 `chatTurnCost`)를 12px 보조 전경색으로 표시. 위쪽 화살표·랜덤 전송·스피너 상태에서도 같은 위치 유지. 게스트에게는 미표시 | ✅ e2e `chats/chat-room` | §3-1-5 입력창과 선택지, §3-1-7 이프 정책 수치 표시, KNK-1045·1095 |

## CHAT-BLOCK — 블럭 입력 모드 (기본)

기본 입력 모드는 블럭 모드입니다. 모드는 `localStorage`에 저장되어 유지됩니다.

| ID            | P   | 사전조건                             | 절차                             | 기대 결과                                                                                          | 자동화                   | 근거                                 |
| ------------- | --- | ------------------------------------ | -------------------------------- | -------------------------------------------------------------------------------------------------- | ------------------------ | ------------------------------------ |
| CHAT-BLOCK-01 | P1  | 첫 진입(모드 저장 없음)              | 입력 영역 확인                   | 블럭 모드가 기본. 묘사("어떤 상황을 묘사할까요?")·대사("어떤 대사를 건넬까요?") 블럭이 하나씩 표시 | ✅ e2e `chats/chat-room` | US-6-15, §3-1-5 입력 모드              |
| CHAT-BLOCK-02 | P0  | 블럭 모드                            | 상황·대사 블럭 입력 → 전송       | `{"userInput":"*상황*\n\n대사"}` 형태의 한 메시지로 직렬화 전송                                    | ✅ e2e `chats/chat-room` | US-6-15, §3-1-5                        |
| CHAT-BLOCK-03 | P2  | 블럭 모드                            | 블럭의 X(입력 삭제) 버튼 탭      | 해당 블럭 인풋이 제거됨                                                                            | ✅ e2e `chats/chat-room` | §3-1-5                                 |
| CHAT-BLOCK-04 | P1  | 블럭 모드, 추천에 `*상황* 대사` 혼합 | 추천 "채우기" 탭                 | 상황은 묘사 블럭에, 대사는 대사 블럭에 파싱되어 채워짐                                             | ✅ e2e `chats/chat-room` | §3-1-5                                 |
| CHAT-BLOCK-05 | P1  | 블럭에 작성 중 내용 있음             | 입력 모드 메뉴에서 일반 모드로 전환 | 작성 내용이 일반 텍스트로 변환되어 유지됨(역방향 전환도 동일)                                      | 수동                     | US-6-16, 구현(`use-chat-composer`)   |
| CHAT-BLOCK-06 | P2  | 모드를 일반으로 변경 후              | 새로고침·재진입                  | 저장된 모드(일반)로 시작. 저장 값이 손상돼도 기본(블럭)으로 동작                                   | 수동                     | US-6-16, 구현(`use-chat-input-mode`) |
| CHAT-BLOCK-07 | P1  | 블럭/일반 각 모드                    | 입력 모드 메뉴(설정 아이콘) 탭 — 블럭 모드는 "대사 추가" 오른쪽, 일반 모드는 "상황 추가" 오른쪽 | 드랍다운에 블럭/일반 라디오 항목(라벨+설명) 표시, 현재 모드 체크. 선택 시 입력창 전환 | ✅ e2e `chats/chat-room`·`visual/chats-visual` | US-6-16, 구현(`chat-input-mode-menu`) |
| CHAT-BLOCK-08 | P2  | 블럭 모드, 블럭 2개 이상             | 키보드 Tab으로 블럭 사이 이동    | X(입력 삭제) 버튼은 탭 순서에서 제외되어 포커스가 블럭 인풋 사이를 바로 오감(터치·클릭으로는 계속 삭제 가능) | 수동                     | 구현(`chat-block-input`)             |
| CHAT-BLOCK-09 | P2  | 블럭 모드                            | 입력 블럭과 "상황 추가"·"대사 추가" 액션 행 사이 확인 | 액션 행의 상단 패딩이 0px로 입력 블럭 바로 아래에 붙고 좌우·하단 16px 여백은 유지 | ✅ e2e `chats/chat-room` | 구현(`chat-block-input`), KNK-1012 |

## CHAT-SET — 옵션 메뉴·채팅 삭제

헤더 우측 옵션 메뉴(⋮)가 드랍다운을 엽니다. 항목은 회원에게 "신고하기" → "삭제하기", 게스트에게 "삭제하기"만이며, 삭제는 낙관적 삭제(§3-1-6) 규칙을 따릅니다(채팅 목록 카드의 옵션 다이얼로그도 같은 절차 — CHAT-LIST-18). 공유 발급은 이 메뉴가 아니라 헤더 공유 버튼이 담당합니다(CHAT-SHARE, KNK-715).

| ID          | P   | 사전조건                  | 절차                                             | 기대 결과                                                                            | 자동화                               | 근거              |
| ----------- | --- | ------------------------- | ------------------------------------------------ | ------------------------------------------------------------------------------------ | ------------------------------------ | ----------------- |
| CHAT-SET-01 | P1  | 게스트 채팅               | 옵션 메뉴(⋮) 버튼 탭                             | 드랍다운 표시: destructive "삭제하기" 항목 하나(공유 항목은 헤더 버튼으로 이동, 문구도 "채팅 삭제하기"에서 축약 — KNK-715). 게스트에게 신고하기는 없음 | ✅ e2e `chats/chat-room`·`visual/chats-visual` | §3-1-5, KNK-715·1186 |
| CHAT-SET-08 | P0  | 회원 채팅                 | 옵션 메뉴(⋮) → "신고하기" → 사유 선택 → 전송     | 메뉴 순서는 "신고하기" → destructive "삭제하기". 시트의 대상은 이 채팅이 참조하는 스토리(`POST /stories/{storyId}/reports`)이고, 201이면 "신고가 접수되었어요" 토스트 + 시트 닫힘. 시트 상세 동작은 STORY-DETAIL-29~31 | ✅ e2e `chats/chat-room`             | §3-1-5 옵션 메뉴, §3-1-3 스토리 신고, KNK-1186 |
| CHAT-SET-02 | P0  | 게스트 채팅               | 삭제하기 → 다이얼로그("채팅을 삭제할까요?") 확정 | "채팅이 삭제되었어요" 토스트 + `/chats`로 replace 이동. 로컬 ID 제거로 목록에서 사라짐 | ✅ e2e `chats/chat-room`             | US-5-3, §3-1-5·§3-1-6 |
| CHAT-SET-03 | P1  | 회원 채팅                 | 삭제 확정                                        | 완료 토스트 + 목록 복귀 시 회원 목록에서도 사라짐(회원 쿼리 무효화)                  | ✅ e2e `chats/chat-room`             | US-5-3            |
| CHAT-SET-04 | P2  | 이미 서버에서 삭제된 채팅 | 삭제 확정(404 응답)                              | 이미 삭제된 것으로 간주해 토스트 없이 목록 제거 유지(무음 성공)                      | 수동                                 | §3-1-6 쓰기·삭제    |
| CHAT-SET-05 | P1  | 삭제 API가 5xx로 실패     | 삭제 확정                                        | 로컬 목록 롤백(카드 복원) + 실패 토스트                                              | 수동                                 | §3-1-6 쓰기·삭제    |
| CHAT-SET-06 | P2  | 삭제 다이얼로그 표시됨    | 취소 선택                                        | 삭제되지 않고 화면 유지                                                              | 수동                                 | §3-1-5              |
| CHAT-SET-07 | P2  | 삭제 요청 진행 중         | 삭제 확정 직후 다이얼로그 관찰                   | 확정 버튼은 문구 공간을 유지한 중앙 스피너("삭제 중")로 전환되고, 취소·확정 버튼이 모두 잠김 | 수동                                 | §3-1-5, 구현(`options-menu`) |

## CHAT-SHARE — 채팅 공유 발급

헤더 우측 공유 버튼(share 아이콘, 접근 이름 "채팅 공유하기 버튼")이 확인 다이얼로그를 열고, 다이얼로그의 "채팅 공유하기" 확정이 `POST /chats/{id}/shares`로 공유 링크를 발급해 클립보드에 복사하고 완료 토스트를 표시합니다(KNK-715). 발급은 멱등이라 같은 커트라인(진행 턴 수)에서 다시 호출하면 같은 링크가 나오고, 턴이 진행된 뒤 호출하면 새 링크가 생기며 기존 링크도 계속 유효합니다.

| ID            | P   | 사전조건                       | 절차                          | 기대 결과                                                                      | 자동화                    | 근거                   |
| ------------- | --- | ------------------------------ | ----------------------------- | ------------------------------------------------------------------------------ | ------------------------- | ---------------------- |
| CHAT-SHARE-01 | P0  | 임의 채팅                      | 헤더 공유 버튼 탭             | 다이얼로그 표시: 제목 "이 채팅을 공유할까요?", 설명 "이 링크가 있으면 누구든지 내가 지금까지 이어온 채팅을 볼 수 있어요", 하단 버튼 두 개(왼쪽 "나중에 하기" 보조, 오른쪽 "링크 복사하기" 주 스타일). 발급 요청은 아직 없음 | ✅ e2e `chats/chat-share` | §3-1-5, KNK-715 |
| CHAT-SHARE-06 | P0  | CHAT-SHARE-01 다이얼로그 표시됨 | "링크 복사하기" 탭            | 발급 요청 후 `/share/{shareId}` 링크가 클립보드에 복사되고 "채팅 공개 링크가 복사되었어요" 토스트(§3-1-5, 정본 `toast-message.ts`) 표시 + 다이얼로그 닫힘 | ✅ e2e `chats/chat-share` | 구현(`use-chat-share`) |
| CHAT-SHARE-07 | P1  | CHAT-SHARE-01 다이얼로그 표시됨 | "나중에 하기" 탭(또는 배경 탭·ESC) | 발급 요청 없이 다이얼로그만 닫힘                                                | ✅ e2e `chats/chat-share`("나중에 하기"만) | §3-1-5, KNK-715 |
| CHAT-SHARE-08 | P2  | 발급 요청 진행 중              | 다이얼로그 관찰               | 확정 버튼이 문구 공간을 유지한 중앙 스피너("복사 중")로 전환되고 두 버튼·배경 닫기가 잠김 | 수동                      | §3-1-5                   |
| CHAT-SHARE-02 | P1  | 발급 API가 403·5xx로 실패      | 다이얼로그에서 "링크 복사하기" 탭 | "공유 링크 생성에 실패했어요" 토스트. 클립보드는 덮어써지지 않고 다이얼로그 유지(재시도 가능) | 수동                      | 구현(`use-chat-share`) |
| CHAT-SHARE-03 | P2  | 같은 채팅, 턴 진행 없이 재공유 | 공유 확정 두 번 실행         | 같은 `shareId` 링크가 나옴(멱등)                                                | 수동                      | 백엔드 §4-3-11         |
| CHAT-SHARE-04 | P2  | 공유 후 턴을 더 진행           | 공유 확정 재실행             | 새 커트라인의 새 링크가 생기고, 먼저 만든 링크도 계속 열림(당시 턴까지만 보임)  | 수동                      | 백엔드 §4-3-11         |
| CHAT-SHARE-05 | P2  | 게스트가 만든 채팅             | 공유 확정                    | 로그인 없이도 발급됨                                                            | 수동                      | 백엔드 §4-3-11         |

## SHARE-VIEW — 공유된 채팅 열람 `/share/[shareId]`

무인증 화면입니다. 추측 불가한 UUID 링크 보유가 접근 수단이며, 온보딩 게이트 매처(`/`·`/chats`·`/my`) 밖이라 게이팅이 없습니다. 발급 시점 커트라인 이하의 턴만 보이고, 스토리 제목·프롤로그는 조회 시점의 라이브 값입니다.

| ID           | P   | 사전조건                             | 절차                         | 기대 결과                                                                                             | 자동화                       | 근거                            |
| ------------ | --- | ------------------------------------ | ---------------------------- | ------------------------------------------------------------------------------------------------------ | ---------------------------- | ------------------------------- |
| SHARE-VIEW-01 | P0  | 유효한 공유 링크, 로그아웃 상태     | 링크 진입                    | 스토리 제목(헤더) + 본문 최상단 중앙 "친구가 공유한 채팅을 보고 있어요" 안내 + 프롤로그 → 사용자 입력 → AI 출력 순 말풍선 표시. 온보딩으로 튕기지 않음 | ✅ e2e `share/shared-chat`  | 구현(`shared-chat-screen`)      |
| SHARE-VIEW-02 | P1  | SHARE-VIEW-01                        | 화면 구성 확인               | 입력창·선택지·재생성 버튼 없음(읽기 전용). 헤더 좌측에 마냑 로고(홈 링크)와 스토리 제목이 나란히. 하단에 "나만의 스토리 만들고 채팅하기" CTA | ✅ e2e `visual/share-visual` | 구현(`shared-chat-view`)        |
| SHARE-VIEW-03 | P1  | SHARE-VIEW-01                        | 헤더 마냑 로고 탭            | 홈(`/`)으로 이동. CTA와 달리 온보딩 게이트를 닫지 않으므로 신규 방문자는 온보딩을 거침                  | ✅ e2e `share/shared-chat`  | 구현(`shared-chat-view`)        |
| SHARE-VIEW-04 | P2  | SHARE-VIEW-01                        | 본문을 탭하거나 스크롤       | 헤더·CTA는 채팅방과 같이 위아래에 고정된 채 반응하지 않고 본문만 스크롤됨. 스크롤바가 헤더·CTA에 가려지지 않고, 본문 하단 fade 바로 아래에 추가 상단 패딩 없이 CTA 푸터가 붙음 | ✅ e2e `share/shared-chat` | 구현(`shared-chat-view`), KNK-1012 |
| SHARE-VIEW-05 | P0  | 잘못됐거나 삭제된 링크(404)          | 링크 진입                    | "공유된 채팅을 찾을 수 없어요" + "삭제되었거나 잘못된 링크예요" + "마냑 둘러보기" 버튼                          | ✅ e2e `share/shared-chat`  | 구현(`shared-chat-screen`)      |
| SHARE-VIEW-06 | P1  | 공유한 뒤 원본 채팅을 삭제           | 이미 보낸 링크로 진입        | SHARE-VIEW-05와 같은 화면(원본을 지우면 공유도 함께 사라짐)                                             | 수동                         | 백엔드 확인(2026-07-29)         |
| SHARE-VIEW-07 | P1  | 열람 API가 5xx로 실패                | 링크 진입                    | "채팅을 불러오지 못했어요" + "다시 시도하기". 재시도 성공 시 정상 표시                                  | 수동                         | 구현(`shared-chat-screen`)      |
| SHARE-VIEW-08 | P1  | 유효한 공유 링크                     | 카카오톡·페이스북 등에 링크 붙여넣기 | 미리보기 카드에 스토리 제목·프롤로그 앞부분(100자)·스토리 썸네일 표시. 썸네일이 없는 스토리는 브랜드 기본 이미지 | 수동                         | 구현(`generateMetadata`)        |
| SHARE-VIEW-09 | P2  | 유효한 공유 링크                     | 페이지 소스의 robots 메타 확인 | `noindex`가 없고 검색 색인이 허용됨                                                                  | 수동                         | 구현(`generateMetadata`)        |
| SHARE-VIEW-09A | P2 | 잘못됐거나 삭제된 링크 또는 메타데이터 조회 실패 | 페이지 소스의 robots 메타 확인 | `noindex, nofollow`. 조회할 수 없는 공유 URL은 검색 결과에 남지 않음                                  | 수동                         | 구현(`generateMetadata`)        |
| SHARE-VIEW-10 | P2  | 유효한 공유 링크                     | 브라우저 탭 제목 확인        | 데이터 도착 후 `스토리 제목 - 마냑`. 서버 메타데이터 조회가 실패해도 채팅 화면과 같은 형식으로 맞춰짐   | ✅ e2e `share/shared-chat`   | 구현(`shared-chat-screen`)      |
| SHARE-VIEW-11 | P1  | 공유 링크로 처음 방문(쿠키 없음)     | CTA "나만의 스토리 만들고 채팅하기" 탭 | 스토리 생성 퍼널(`/studio/story/simple`)로 바로 이동. 이후 홈에 들어가도 온보딩이 뜨지 않음(열람 화면에서 이미 서비스를 봤으므로) | ✅ e2e `share/shared-chat`  | 설계 결정                       |
| SHARE-VIEW-12 | P1  | SHARE-VIEW-11 직후                   | 생성 퍼널에서 뒤로가기       | 진입 경로와 관계없이 제작(`/studio`) 화면으로 이동                                                                            | 수동                         | 구현(`use-story-create-funnel`), KNK-988 |

## CHAT-REGEN — 응답 재생성 `미배포`

`POST /chats/{id}/turns/regenerate/stream {turnId}`. dev에 구현되어 있으나 v0.2.2 릴리스에는 미포함입니다.

| ID            | P   | 사전조건                            | 절차                 | 기대 결과                                                                                         | 자동화                             | 근거                  |
| ------------- | --- | ----------------------------------- | -------------------- | ------------------------------------------------------------------------------------------------- | ---------------------------------- | --------------------- |
| CHAT-REGEN-01 | P1  | 턴 2개 이상 채팅                    | 각 AI 버블 하단 확인 | "다시 생성" 버튼은 마지막 턴에만 표시. `aiOutput`이 비었거나 `reachedEnding`이 있는 턴에는 미표시 | ◐ e2e `chats/chat-room`(엔딩 턴만) | US-6-10, §3-1-5 재생성  |
| CHAT-REGEN-02 | P0  | 마지막 턴 존재                      | 다시 생성 탭         | 기존 AI 본문·선택지가 스트리밍 블록으로 대체 → 완료 시 새 본문 표시. 사용자 입력 버블은 유지      | ✅ e2e `chats/chat-room`           | US-6-10, §3-1-5         |
| CHAT-REGEN-03 | P1  | 재생성 중 서버 `error` 이벤트       | 다시 생성            | 기존 본문·선택지 복원 + "응답 생성에 실패했어요" 토스트. 다시 생성 버튼 재표시                    | ✅ e2e `chats/chat-room`           | §3-1-5 재생성 실패 처리 |
| CHAT-REGEN-04 | P1  | 재생성 중 네트워크 절단·EOF         | 다시 생성            | 임의 복원하지 않고 상세 refetch 결과(서버 확정본)를 표시                                          | 수동                               | §3-1-5 재생성 실패 처리 |
| CHAT-REGEN-05 | P2  | 낡은 화면(다른 탭에서 새 턴 추가됨) | 다시 생성(서버 409)  | 상세 refetch로 최신 상태 반영. 오류 토스트 없음                                                   | 수동                               | §3-1-5 재생성 실패 처리 |
| CHAT-REGEN-06 | P2  | 스트리밍(전송·재생성) 진행 중       | 다시 생성 버튼 확인  | 전송 잠금과 동일하게 비활성화                                                                     | 수동                               | §3-1-5 재생성           |
| CHAT-REGEN-07 | P2  | 마지막 턴이 엔딩 도달 턴(`reachedEnding` 존재) | AI 버블 하단 확인    | "다시 생성" 버튼이 표시되지 않는다(서버도 409로 거부). 입력·선택지는 정상 동작(CHAT-SEND-16)                                                              | ✅ 유닛(`regenerate`)          | §3-1-5 재생성 버튼, US-6-14 |
| CHAT-REGEN-08 | P2  | 마지막 턴에 재생성 버튼과 추천 입력 존재 | 두 요소 사이 확인 | 재생성 버튼 터치 영역 아래 20px + 추천 입력 목록 위 12px으로 첫 추천 입력까지 총 32px 간격 | ✅ e2e `chats/chat-room` | §3-1-5 재생성 버튼, KNK-1029 |

## CHAT-LIMIT — 게스트 체험 한도·이프

게스트 채팅 한도는 전 채팅방 합산 5턴입니다. 클라이언트는 로컬 카운터(`manyak:guest-usage`)로 선차단하고, 서버 402는 게스트에게 로그인 모달 바텀 시트, 회원에게 이프 부족 토스트로 안내합니다. 재생성도 동일한 게이팅을 탑니다(KNK-1045).

| ID            | P   | 사전조건                                            | 절차                   | 기대 결과                                                                                                                   | 자동화                          | 근거                                      |
| ------------- | --- | --------------------------------------------------- | ---------------------- | --------------------------------------------------------------------------------------------------------------------------- | ------------------------------- | ----------------------------------------- |
| CHAT-LIMIT-01 | P0  | 게스트, 로컬 카운터가 한도(5) 도달                  | 전송 시도              | 요청 없이 로그인 유도 바텀 시트 표시. 입력 내용은 유지                                                                      | ✅ e2e `chats/chat-guest-limit` | US-10-5, §3-1-5 예외 처리                   |
| CHAT-LIMIT-02 | P0  | 게스트, 서버 판정 한도 초과(로컬 카운터 미달이어도) | 전송(서버 402)         | 낙관적 입력 버블 제거 + 로그인 유도 바텀 시트(토스트 아님)                                                                  | ✅ e2e `chats/chat-guest-limit` | US-10-5, §3-1-5                             |
| CHAT-LIMIT-03 | P0  | 회원, 서버가 이프 부족 402 응답                   | 전송                    | 낙관적 입력 버블 제거 + 현재 화면에 "이프가 부족해요" 오류 토스트만 표시. 다이얼로그·화면 이동 없이 기존 대화 유지       | ✅ e2e `chats/chat-guest-limit` | US-10-4, §3-1-5, KNK-1045                   |
| CHAT-LIMIT-04 | P1  | 게스트/회원 각각 한도 상태                          | 다시 생성 시도         | 게스트는 로그인 바텀 시트, 회원은 이프 부족 토스트. 402 시 기존 본문 유지                                                | 수동                            | §3-1-5 재생성 실패 처리                     |
| CHAT-LIMIT-06 | P2  | 게스트 턴 1회 완료                                  | 로컬 카운터 확인       | `completed` 수신 시에만 +1(실패 턴은 미집계). 로그인·이관 후 카운터 제거                                                    | 수동                            | 구현(`guest-usage-storage`)               |

## ⚠️ 확인 필요

작성 중 스펙과 구현이 어긋나거나 판단이 필요한 항목입니다. 팀 확인 후 스펙 갱신 또는 수정 이슈로 전환합니다.

해소된 항목: 재생성·이프 402 안내 UI 라벨 드리프트 — 스펙(§3-1-5)이 `Phase 1 · 구현`으로 정렬 완료(재생성은 `미배포` 표기만 유지).

1. **채팅 스트림의 클라이언트 타임아웃 부재(G5 잔여).** 전송 경로의 EOF 미처리는 KNK-741로 해소됐습니다(CHAT-SEND-11). 다만 채팅 스트림은 공용 요청 계층(`fetchWithTimeout`)을 우회해 맨 `fetch`를 쓰므로 클라이언트 상한이 없고, FIN 없이 끊긴 연결은 백엔드 상한에만 기댑니다. 도입 여부는 팀 판단이 필요합니다(§3-1-9 G5).
2. **채팅 배경 이미지 미구현.** 인물 이미지는 웹 렌더까지 구현되어 CHAT-IMAGE에서 검증합니다(KNK-1013·1015). 별도 배경 이미지 마커 트랙은 서버·AI·웹 모두 `Phase 1 · 계획`이라 아직 QA 범위에서 제외합니다.
3. **게스트 한도 로컬 카운터의 우회 가능성.** 로컬 카운터는 UX용 선차단이고 실제 강제는 서버 402입니다. 로컬스토리지를 지우면 선차단이 풀리지만 서버가 차단하므로 정책상 문제 없는지(다이얼로그 문구가 동일한지) CHAT-LIMIT-02로 확인이 필요합니다.
4. ~~**채팅 비용 표시값과 서버 정책이 다름.**~~ 해소 — KNK-1095로 컴포저가 `GET /credits/policies`의 `chatTurnCost`를 렌더하므로 표시값이 서버 정책과 갈라질 수 없습니다(CHAT-INPUT-20).
